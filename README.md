# NextAge - Game Library Manager

A cross-platform game library manager built with React and TypeScript.

## Prerequisites

- Node.js 18+
- PostgreSQL (for backend)
- Docker (optional, for backend database)

## Setup

### 1. Install Dependencies

```bash
npm install
